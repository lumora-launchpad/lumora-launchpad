[Unit]
Description=Lumora off-chain data backup (web/.data)
After=network.target

[Service]
Type=oneshot
ExecStart=/root/lumora-launchpad/scripts/backup-data.sh
# Adjust BACKUP_DIR or KEEP here if you want non-default values.
# Environment=BACKUP_DIR=/root/lumora-backups
# Environment=KEEP=14
